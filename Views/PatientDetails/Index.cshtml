@model IEnumerable<ck283615_MIS4200.Models.PatientDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Operation.operationDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.insuranceCompany)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.amountOwed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Operation.operationDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.insuranceCompany)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.amountOwed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.patientdetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.patientdetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.patientdetailID })
        </td>
    </tr>
}

</table>
